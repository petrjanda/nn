import matBuilder from '../../../utils/mat-builder';
import { module, test } from 'qunit';

module('matBuilder');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = matBuilder();
  assert.ok(result);
});
